apiVersion: v1
kind: Service
metadata:
  labels:
    app: bonusengine
    environment: Development
  name: bonusengine
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: bonusengine
  sessionAffinity: None
  type: ClusterIP
